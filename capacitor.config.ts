import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crafter8.bedrock_world_editor',
  appName: 'Bedrock World Editor',
  webDir: '.vite/renderer/main_window'
};

export default config;
